# MamaSentry local development configuration
APP_ENV=development
HOST=127.0.0.1
PORT=8000

# Optional future AI provider configuration.
# Keep real credentials in .env only; never commit them.
AI_PROVIDER=none
AI_API_KEY=